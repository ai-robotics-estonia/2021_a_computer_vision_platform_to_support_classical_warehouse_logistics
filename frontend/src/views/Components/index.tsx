import React from 'react';

import MainView from '../../components/MainView';

import './style.scss';

const Components = () => {
  return (
    <MainView>
      <MainView.Header title="Components"/>
      <MainView.Content/>
    </MainView>
  );
};

export default Components;
